.venv/
__pycache__/
node_modules/
.env
.env.*
!.env.example

# Production secrets & certificates (НИКОГДА не коммитить!)
certbot/
backups/
*.pem
*.key

# SSH keys (НИКОГДА не коммитить!)
diplom
diplom.pub
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub

# Claude Code harness worktrees / session metadata
.claude/
.claude/**
*.pyc
*.sqlite3
.DS_Store
/dist
/.next
# Next.js build artifacts inside frontend
frontend/.next/

# Backend coverage/junit artifacts
backend/coverage.xml
backend/junit.xml
backend/.coverage
# Ignore collected static files
backend/staticfiles/

# Exercises deliverables (PDF exports)
docs/*.pdf
.github/copilot-instructions.md

# Claude Code service instruction file (local only, не для команды)
CLAUDE.md

# Diploma explanatory note (Word docs) — рукопись держится отдельно от кода
*.docx
_diplom_chapters/
