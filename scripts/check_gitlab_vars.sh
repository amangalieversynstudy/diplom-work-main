#!/usr/bin/env bash
# Placeholder removed per request.
# The project no longer uses this script; CI references were updated to not call it.
echo "check_gitlab_vars.sh has been intentionally removed and replaced with this placeholder."
exit 0
